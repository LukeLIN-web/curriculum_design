list_is_empty：判断一个表是否为空表，空表返回非零值，非空返回零。

list_is_last：判断一个节点是否为表的尾节点，尾节点返回非零值，否则返回零。

list_free：释放整个表内存。

list_prepend：在表头插入节点。

list_append：在表尾插入节点。

list_next：获取表中下一个节点。

list_find：找到表中数据。

list_nth_data：获得表中的第n个数据。

list_data：返回节点中存储的数据。

list_length：得到表的长度，返回链表中节点的个数。

list_remove_entry：删除一个节点，成功删除返回非零数，节点不存在返回零。

list_insert：向表中插入节点。